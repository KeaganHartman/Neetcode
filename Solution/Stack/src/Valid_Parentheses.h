#pragma once
#include "includes.h"
class Valid_Parentheses
{
public:
	Valid_Parentheses();

private:
	//	Given a string (s) contiaining just the characters " () {} [] ", determine if the input string is valid.
	bool isValid(string s);
};

